<?php

test('you can start a job stack', function () {
    //
});

test('you can finish a job stack', function () {
    //
});

test('you can fail a job stack', function () {
    //
});

test('you can append a job onto the job stack', function () {
    //
});

test('you can get the next job in the job stack', function () {
    // Make separate method for it
});

test('you can dispatch the next job in the job stack', function () {
    //
});

test('you can dispatch the next job in the job stack with a custom delay', function () {
    //
});
