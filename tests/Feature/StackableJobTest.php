<?php

test('a stackable job can call the next job', function () {

});

test('a stackable job can append another job', function () {

});

test('a stackable job can finish a job stack', function () {

});

test('a stackable job can fail a job stack', function () {

});

test('when a stackable job is created the job stack is loaded', function () {

});
