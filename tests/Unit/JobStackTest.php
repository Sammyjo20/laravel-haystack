<?php

test('a job stack can have many job stack rows', function () {
    //
});

test('a job stack row can belong to a job stack', function () {
    //
});

test('a job stacks rows are ordered by their id', function () {
    //
});

test('you can store a serialized closure on a job stack', function () {
    //
});

test('you can store an invokable class on a job stack', function () {
    //
});

test('when a job stack is finished it will delete itself and all other rows', function () {
    //
});

test('when a job stack fails it will delete itself and all other rows', function () {
    //
});

test('you can instantiate a job stack builder from the model', function () {
    //
});

test('you can use a custom model for the job stack', function () {
    //
})->skip();

test('you can use a custom model for the job stack row', function () {
    //
})->skip();
